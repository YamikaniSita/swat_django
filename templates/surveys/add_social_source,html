{% extends "base.html" %}
{% load crispy_forms_tags %}

{% block title %}
    {% if survey %}Edit Survey{% else %}Create Survey{% endif %}
{% endblock %}

{% block content %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        {% if survey %}Edit Survey{% else %}Create New Survey{% endif %}
                    </h5>
                </div>
                <div class="card-body">
                    <form method="post">
                        {% csrf_token %}
                        {{ form|crispy }}
                        
                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> Save Survey
                            </button>
                            <a href="{% url 'core:project_list' %}" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Back to Projects
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %} 